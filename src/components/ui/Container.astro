---
---
<div class="ui-container">
  <slot />
</div>

<style>
  .ui-container {
    max-width: var(--layout-max-width);
    margin-inline: auto;
    padding-inline: var(--spacing-container);
  }
</style>